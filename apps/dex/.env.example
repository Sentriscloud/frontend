NEXT_PUBLIC_RPC_URL=
NEXT_PUBLIC_DEX_NETWORK=mainnet

# Filled only after the official Hyperlane Base USDC -> Sentrix sUSDC route is
# deployed and verified. Leave empty to keep sUSDC hidden from the DEX token
# picker. Never use placeholder token addresses in production builds.
NEXT_PUBLIC_SUSDC_MAINNET_ADDRESS=
NEXT_PUBLIC_SUSDC_TESTNET_ADDRESS=
